<h1>Companies</h1>

<% current_user.companies.each do |company| %>
  <div class="row">
    <div class="column">
      <span class="button button-outline"><%= link_to company.name, user_company_path(current_user, company) %></span>
    </div>
  </div>
<% end %>